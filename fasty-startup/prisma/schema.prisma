// ============================================
// PRISMA SCHEMA
// ============================================
// This file is DYNAMIC - you MUST customize it for your project
// Only the User model below is templatable (most apps need users)
//
// After changes, run:
//   npm run db:generate   (regenerate Prisma client)
//   npm run db:migrate    (create migration)
// ============================================

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================
// TEMPLATABLE: Most apps need this
// ============================================

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  role      Role     @default(USER)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Add your relations here:
  // posts     Post[]
  // orders    Order[]
}

enum Role {
  USER
  ADMIN
}

// ============================================
// YOUR DOMAIN MODELS GO HERE
// ============================================
//
// Example for e-commerce:
//
// model Product {
//   id          String   @id @default(cuid())
//   name        String
//   price       Decimal  @db.Decimal(10, 2)
//   description String?
//   createdAt   DateTime @default(now())
//   updatedAt   DateTime @updatedAt
// }
//
// model Order {
//   id        String      @id @default(cuid())
//   status    OrderStatus @default(PENDING)
//   total     Decimal     @db.Decimal(10, 2)
//   userId    String
//   user      User        @relation(fields: [userId], references: [id])
//   items     OrderItem[]
//   createdAt DateTime    @default(now())
// }
//
// enum OrderStatus {
//   PENDING
//   CONFIRMED
//   SHIPPED
//   DELIVERED
//   CANCELLED
// }
